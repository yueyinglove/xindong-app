[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "xindong"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "requests",
]
